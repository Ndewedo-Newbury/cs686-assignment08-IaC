---
- name: Update packages, verify Docker, and report disk usage
  hosts: all
  become: true
  gather_facts: true

  tasks:

    # --- Update & upgrade packages ---

    - name: Update and upgrade packages (Amazon Linux)
      ansible.builtin.yum:
        name: "*"
        state: latest
      when: ansible_os_family == "RedHat"

    - name: Update and upgrade packages (Ubuntu)
      ansible.builtin.apt:
        upgrade: dist
        update_cache: true
      when: ansible_os_family == "Debian"

    # --- Verify Docker version ---

    - name: Get Docker version
      ansible.builtin.command: docker --version
      register: docker_version
      changed_when: false

    - name: Report Docker version
      ansible.builtin.debug:
        msg: "{{ inventory_hostname }} — {{ docker_version.stdout }}"

    # --- Disk usage ---

    - name: Report disk usage
      ansible.builtin.debug:
        msg: >
          {{ inventory_hostname }} — {{ item.mount }}:
          {{ item.size_used | human_readable }} used of
          {{ item.size_total | human_readable }}
          ({{ (item.size_used / item.size_total * 100) | round(1) }}%)
      loop: "{{ ansible_mounts }}"
      when: item.size_total > 0
